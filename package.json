{
  "name": "moneyFakerBackend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "schema:generate": "bun drizzle-kit generate:sqlite",
    "schema:migrate": "bun run src/migrate.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.3.6",
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.29.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.1"
  },
  "module": "src/index.js"
}
